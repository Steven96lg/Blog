---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import { getCollection } from 'astro:content';

const ciberseguridadPosts = (await getCollection('ciberseguridad')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<Layout>
	<Navigation />
	<main>
		<div class="general-content">
			<div class="ciberseguridad-header">
				<h1>🔒 Roadmap de Ciberseguridad</h1>
				<p class="header-description">Mi viaje y aprendizaje en el mundo de la ciberseguridad. Aquí documento mis avances, descubrimientos y las mejores prácticas que voy adquiriendo.</p>
			</div>
			
			<div class="ciberseguridad-posts">
				{ciberseguridadPosts.map((post) => (
					<article>
						<div class="post-card">
							<h2><a href={`/blog/ciberseguridad/${post.slug}`}>{post.data.title}</a></h2>
							<div class="post-meta">
								<span class="date">
									<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24">
										<g fill="none">
											<rect width="18" height="15" x="3" y="6" stroke="currentColor" stroke-width="2" rx="2"/>
											<path fill="currentColor" d="M3 10c0-1.886 0-2.828.586-3.414S5.114 6 7 6h10c1.886 0 2.828 0 3.414.586S21 8.114 21 10z"/>
											<path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7 3v3m10-3v3"/>
										</g>
									</svg>
									{new Date(post.data.pubDate).toLocaleDateString('es-MX', { year: 'numeric', month: 'short', day: 'numeric' })}
								</span>
								<span class="tag">{post.data.tag}</span>
							</div>
							<p>{post.data.description}</p>
							<a href={`/blog/ciberseguridad/${post.slug}`} class="read-more">Leer más →</a>
						</div>
					</article>
				))}
			</div>

			{ciberseguridadPosts.length === 0 && (
				<div class="no-posts">
					<p>Pronto habrá contenido sobre ciberseguridad. ¡Mantente atento! 🚀</p>
				</div>
			)}
		</div>
	</main>
</Layout>

<style>
	main {
		width: 1250px;
		margin: 0 auto;
	}

	.general-content {
		padding-top: 100px;
		padding-bottom: 50px;
		font-family: "Archivo Narrow", sans-serif;
	}

	.ciberseguridad-header {
		padding: 40px 20px;
		border-radius: 10px;
		background: rgba(236, 245, 253, 0.4);
		margin-bottom: 40px;
	}

	.ciberseguridad-header h1 {
		color: rgb(54, 54, 54);
		margin: 0 0 15px 0;
		font-size: 36px;
	}

	.header-description {
		color: gray;
		font-family: Arial, Helvetica, sans-serif;
		font-size: 15px;
		line-height: 1.6;
		margin: 0;
	}

	.ciberseguridad-posts {
		display: flex;
		flex-direction: column;
		gap: 30px;
	}

	.post-card {
		background: white;
		padding: 30px;
		border-radius: 8px;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
		transition: all 0.3s ease;
	}

	.post-card:hover {
		box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
		transform: translateY(-2px);
	}

	.post-card h2 {
		margin: 0 0 15px 0;
		font-size: 24px;
	}

	.post-card h2 a {
		color: black;
		text-decoration: none;
		transition: color 0.3s ease;
	}

	.post-card h2 a:hover {
		color: rgb(44, 146, 172);
	}

	.post-meta {
		display: flex;
		gap: 20px;
		align-items: center;
		margin-bottom: 15px;
		flex-wrap: wrap;
	}

	.post-meta .date {
		display: flex;
		align-items: center;
		gap: 8px;
		font-size: 14px;
		color: #666;
	}

	.post-meta .tag {
		background-color: black;
		color: white;
		padding: 5px 12px;
		border-radius: 4px;
		font-size: 12px;
		font-weight: 500;
	}

	.post-card p {
		color: #555;
		line-height: 1.6;
		margin: 15px 0;
		font-size: 16px;
	}

	.read-more {
		display: inline-block;
		color: rgb(44, 146, 172);
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
	}

	.read-more:hover {
		color: rgb(28, 110, 132);
		transform: translateX(5px);
	}

	.no-posts {
		text-align: center;
		padding: 60px 20px;
		color: gray;
		font-size: 18px;
	}

	@media (max-width: 480px) {
		main {
			width: 370px;
			padding: 10px;
		}

		.ciberseguridad-header {
			padding: 20px;
		}

		.ciberseguridad-header h1 {
			font-size: 28px;
		}

		.post-card {
			padding: 20px;
		}

		.post-card h2 {
			font-size: 20px;
		}
	}
</style>
